import { AuthLayout } from "../layouts/auth-layout";

export const Login = () => {
  return (
    <AuthLayout>
      {/* <Auth
        supabaseClient={supabase}
        view="sign_up"
        providers={['google']}
        appearance={{ theme: ThemeSupa }}
      /> */}
      log in here
    </AuthLayout>
  );
};
